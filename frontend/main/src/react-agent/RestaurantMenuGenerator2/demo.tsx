

import React from 'react';
import PDFMenuGeneratorPage from './PDFMenuGeneratorPage';

const PDFMenuGeneratorPageDemo: React.FC = () => {
  return (
    <PDFMenuGeneratorPage restaurantList={["Restaurant 1", "Restaurant 2", "Restaurant 3"]} />
  );
};

export default PDFMenuGeneratorPageDemo;

